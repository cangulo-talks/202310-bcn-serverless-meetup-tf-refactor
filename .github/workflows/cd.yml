
name: 🚀 continuous-delivery

on:
  push:
    branches:
      - main

jobs:
  tfm-apply:
    name: 📃 Plan All workspaces
    uses: ./.github/workflows/shared-tf-apply.yml
    with:
      workspaces: "['tst/arepabank', 'acc/arepabank', 'prd/arepabank']"
      apply: true
    secrets: inherit